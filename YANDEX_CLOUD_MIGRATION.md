# Миграция на Yandex Cloud Object Storage

## ✅ Выполнено

### 1. Настройка Yandex Cloud
- Создан бакет: `aey-filesstorage`
- Регион: `ru-central1`
- Настроен CORS для локальной разработки и продакшена
- Приватный доступ к файлам через signed URLs (срок действия: 1 час)

### 2. Обновление кода
Все API endpoints переключены на Yandex Cloud:
- ✅ `/api/photos/upload` - загрузка фотографий
- ✅ `/api/photos/route` - получение фотографий с signed URLs
- ✅ `/api/documents/upload` - загрузка документов
- ✅ `/api/receipts/[id]/upload` - загрузка чеков
- ✅ `/api/design-projects/[id]/upload` - загрузка файлов дизайн-проектов
- ✅ `/api/designer/estimates/[id]/items` - изображения в сметах дизайнеров
- ✅ Все DELETE операции (удаление файлов)

### 3. Новые утилиты
Создан модуль `src/lib/storage.ts` с функциями:
- `uploadFile()` - загрузка файла в Yandex Cloud
- `deleteFile()` - удаление файла из Yandex Cloud
- `getSignedDownloadUrl()` - генерация временной ссылки для скачивания
- `downloadFileAsBuffer()` - скачивание файла (для миграции)

### 4. Миграция существующих файлов
Создан скрипт `scripts/migrate-to-yandex-cloud.ts`:
- Автоматически находит все файлы в БД с URL от Vercel Blob
- Скачивает их и загружает в Yandex Cloud
- Обновляет записи в БД с новыми ключами (keys)
- Обрабатывает ошибки и продолжает работу

**Запуск миграции:**
```bash
npx tsx scripts/migrate-to-yandex-cloud.ts
```

## 📋 Конфигурация

### Переменные окружения (.env)
```env
# Yandex Cloud Object Storage
YC_STORAGE_ACCESS_KEY_ID=YCAJE_...
YC_STORAGE_SECRET_ACCESS_KEY=YCPnz9m_...
YC_STORAGE_BUCKET=aey-filesstorage
YC_STORAGE_REGION=ru-central1
YC_STORAGE_ENDPOINT=https://storage.yandexcloud.net
```

### CORS конфигурация в Yandex Cloud Console
```json
[
  {
    "AllowedOrigins": [
      "http://localhost:3004",
      "https://aeyappfirst.vercel.app"
    ],
    "AllowedMethods": ["GET", "PUT", "POST", "DELETE", "HEAD"],
    "AllowedHeaders": ["*"],
    "ExposeHeaders": ["ETag"],
    "MaxAgeSeconds": 3600
  }
]
```

## 🚀 Как это работает

### Загрузка новых файлов
1. Файл загружается пользователем через форму
2. API endpoint конвертирует файл в Buffer
3. Файл загружается в Yandex Cloud с ключом (path)
4. В БД сохраняется только ключ (например: `photos/client-id/file.jpg`)

### Получение файлов
1. API endpoint читает ключ из БД
2. Если ключ не содержит `http` (новый формат), генерируется signed URL
3. Signed URL действует 1 час
4. Frontend получает временную ссылку для отображения файла

### Преимущества
- ✅ Приватные файлы (не доступны публично)
- ✅ Контроль доступа через signed URLs
- ✅ Хранение в России (быстрее для российских пользователей)
- ✅ Более низкая стоимость по сравнению с Vercel Blob

## 🔄 Статус миграции

### Типы файлов для миграции:
- Фотографии: ~38 файлов
- Документы: ~7 файлов
- Чеки: ~5 файлов
- Файлы дизайн-проектов: ~1 файл
- Изображения смет дизайнера: ~1 файл

**Всего: 52 файла**

### Известные проблемы
- Некоторые файлы из Vercel Blob дают таймауты при скачивании (ETIMEDOUT)
- Скрипт автоматически пропускает проблемные файлы
- Эти файлы можно будет перезалить вручную или повторить миграцию позже

## 📝 Дальнейшие шаги

### 1. После завершения миграции
```bash
# Проверить результаты миграции (в конце вывода скрипта)
# Будет показано:
# ✅ Миграция завершена!
#    Успешно: X
#    Ошибки: Y
#    Всего: 52
```

### 2. Очистка (опционально, после проверки)
- Можно удалить зависимость `@vercel/blob` из `package.json`
- Удалить переменную `BLOB_READ_WRITE_TOKEN` из `.env`
- Удалить файлы из Vercel Blob (через консоль Vercel)

### 3. Деплой на Vercel
```bash
# Добавить переменные окружения в Vercel:
vercel env add YC_STORAGE_ACCESS_KEY_ID
vercel env add YC_STORAGE_SECRET_ACCESS_KEY
vercel env add YC_STORAGE_BUCKET
vercel env add YC_STORAGE_REGION
vercel env add YC_STORAGE_ENDPOINT

# Задеплоить
vercel --prod
```

## 🛠 Troubleshooting

### Ошибка: "Access Denied"
- Проверьте правильность ACCESS_KEY_ID и SECRET_ACCESS_KEY
- Убедитесь, что ключи имеют права на запись в бакет

### Файлы не отображаются
- Проверьте CORS конфигурацию в Yandex Cloud Console
- Убедитесь, что домен добавлен в AllowedOrigins

### Миграция зависла
- Это нормально из-за таймаутов Vercel Blob
- Скрипт продолжит работу, пропуская проблемные файлы
- Можно повторить миграцию позже для пропущенных файлов

## 📚 Полезные ссылки
- [Yandex Cloud Object Storage](https://cloud.yandex.ru/docs/storage/)
- [S3 API Documentation](https://docs.aws.amazon.com/AmazonS3/latest/API/Welcome.html)
- [AWS SDK для JavaScript v3](https://docs.aws.amazon.com/AWSJavaScriptSDK/v3/latest/)

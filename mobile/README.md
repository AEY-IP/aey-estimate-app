# Мобильное приложение "Идеальный подрядчик"

Кроссплатформенное мобильное приложение для рабочих на React Native + Expo.

## 🎯 Функционал (текущая версия)

- ✅ Авторизация по 6-значному PIN-коду
- ✅ Личный кабинет рабочего с информацией об объекте
- ✅ Библиотека технологических карт
- ✅ Детальный просмотр техкарт с пошаговыми инструкциями
- 🔄 Офлайн режим (в разработке)
- 🔄 Синхронизация с веб-приложением (в разработке)

## 📱 Дизайн

Приложение использует единую дизайн-систему с веб-приложением:
- Цвета: розовый (#ec4899) + черный
- Типографика: системный шрифт (iOS/Android)
- Стиль: современный, минималистичный, удобный для работы на объекте

## 🚀 Установка и запуск

### Предварительные требования

```bash
# Проверьте версию Node.js (требуется >= 18)
node --version

# Проверьте npm
npm --version
```

### Установка зависимостей

```bash
cd mobile
npm install
```

### Запуск для разработки

```bash
# Запустить dev сервер
npm start

# Или конкретная платформа
npm run ios      # iOS Simulator (только на Mac)
npm run android  # Android Emulator
npm run web      # Веб-версия (для быстрой разработки)
```

## 📲 Тестирование на реальном устройстве

### iPhone (с Expo Go)

1. **Установите Expo Go на iPhone**
   - Откройте App Store
   - Найдите "Expo Go"
   - Установите приложение

2. **Запустите dev сервер**
   ```bash
   cd mobile
   npm start
   ```

3. **Подключитесь к приложению**
   - В терминале появится QR-код
   - Откройте камеру iPhone и наведите на QR-код
   - Нажмите на уведомление "Открыть в Expo Go"
   - Приложение загрузится и запустится

4. **Важно**: iPhone и компьютер должны быть в одной Wi-Fi сети

### Android (с Expo Go)

1. **Установите Expo Go на Android**
   - Откройте Google Play
   - Найдите "Expo Go"
   - Установите приложение

2. **Запустите dev сервер**
   ```bash
   cd mobile
   npm start
   ```

3. **Подключитесь к приложению**
   - Откройте Expo Go на телефоне
   - Нажмите "Scan QR code"
   - Отсканируйте QR-код из терминала
   - Приложение загрузится и запустится

## 🔑 Тестовые данные

### PIN-код для входа
```
123456
```

### Тестовый рабочий
- Имя: Иван Петров
- Телефон: +7 999 123-45-67
- Объект: ЖК Level Нагатинская

### Доступные техкарты (5 шт.)
1. Устройство стяжки пола
2. Покраска стен
3. Монтаж гипсокартона на стены
4. Укладка керамогранита на пол
5. Установка натяжного потолка

## 📁 Структура проекта

```
mobile/
├── src/
│   ├── screens/           # Экраны приложения
│   │   ├── PinAuthScreen.tsx
│   │   ├── WorkerDashboardScreen.tsx
│   │   └── TechnicalCardDetailScreen.tsx
│   ├── components/        # Переиспользуемые компоненты
│   ├── services/          # API и сервисы
│   │   └── api.ts
│   ├── types/             # TypeScript типы
│   │   └── index.ts
│   ├── constants/         # Константы и моковые данные
│   │   ├── colors.ts
│   │   └── mockData.ts
│   └── utils/             # Утилиты
├── assets/                # Изображения и медиа
├── App.tsx                # Главный файл приложения
└── package.json
```

## 🔄 Следующие шаги

### Интеграция с бэкендом

1. **Создать API endpoints в Next.js**
   ```typescript
   POST /api/auth/worker        // Авторизация по PIN
   GET  /api/technical-cards    // Получить список карт
   GET  /api/technical-cards/:id // Получить детали карты
   ```

2. **Обновить Prisma schema**
   ```prisma
   model Worker {
     id        String   @id @default(cuid())
     name      String
     pin       String   @unique
     clientId  String
     // ...
   }

   model TechnicalCard {
     id          String   @id @default(cuid())
     title       String
     description String   @db.Text
     steps       Json?
     // ...
   }
   ```

3. **Подключить реальный API**
   - Заменить моковые данные в `src/services/api.ts`
   - Добавить обработку ошибок
   - Реализовать токены для авторизации

### Офлайн режим

1. **Установить AsyncStorage**
   ```bash
   npx expo install @react-native-async-storage/async-storage
   ```

2. **Реализовать кэширование**
   - Сохранять техкарты локально
   - Синхронизировать при подключении к сети
   - Показывать индикатор офлайн/онлайн

### Медиа контент

1. **Добавить просмотр фото**
   - Галерея изображений
   - Увеличение по тапу
   - Возможность скачать

2. **Добавить видео плеер**
   - Встроенный проигрыватель
   - Поддержка офлайн воспроизведения

## 📦 Сборка для продакшена

### iOS (App Store)

```bash
# Создать production build
npx eas build --platform ios

# После тестирования submit в App Store
npx eas submit --platform ios
```

**Требования:**
- Apple Developer Account ($99/год)
- Сертификаты и профили provisioning

### Android (Google Play)

```bash
# Создать production build
npx eas build --platform android

# Submit в Google Play
npx eas submit --platform android
```

**Требования:**
- Google Play Developer Account ($25 единоразово)
- Signing key для подписи приложения

## 🐛 Отладка

### Просмотр логов

```bash
# Общие логи
npm start

# React Native логи
npx react-native log-ios      # iOS
npx react-native log-android  # Android
```

### Инструменты разработчика

- React Native Debugger
- Flipper (для продвинутой отладки)
- Expo DevTools (открывается автоматически при `npm start`)

## 💡 Полезные команды

```bash
# Очистить кэш
npx expo start -c

# Обновить зависимости Expo
npx expo install --fix

# Проверить конфигурацию
npx expo-doctor

# Создать build локально (для тестирования)
npx expo prebuild
```

## 📚 Документация

- [Expo Documentation](https://docs.expo.dev/)
- [React Native Documentation](https://reactnative.dev/)
- [React Navigation](https://reactnavigation.org/)
- [EAS Build](https://docs.expo.dev/build/introduction/)

## 🎨 UI компоненты

Текущая версия использует нативные компоненты React Native. В будущем можно добавить:
- React Native Paper (Material Design)
- React Native Elements
- NativeBase
